# Run lint-staged for root (Go files)
npx lint-staged

# Run lint-staged for web directory
cd web && npx lint-staged && cd ..

# Run quick tests (not full coverage)
echo "Running tests..."
npm run test:frontend
npm run test:backend

