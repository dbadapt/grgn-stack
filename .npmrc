# npm configuration

# Use exact versions by default (no ^ or ~)
save-exact=true

# Faster installs
prefer-offline=true

# Security
audit-level=moderate

# Logging
loglevel=warn

# Engine strictness
engine-strict=true
