# services/core/shared/service_config.yaml
# Shared infrastructure app configuration

extends: ../service_config.yaml

app:
  name: shared
  description: "Global infrastructure - database, mailer, storage interfaces"

config:
  # Database configuration
  database:
    maxPoolSize: ${GRGN_DB_MAX_POOL_SIZE:50}
    connectionTimeout: 2m
    
  # Health check configuration
  health:
    enabled: true
    timeout: 2s
