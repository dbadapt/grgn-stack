# GraphQL Schema - Generic Multi-Tenant Application Template
# Go + Neo4j + GraphQL + React Stack

scalar Time
scalar UUID

# Root Query type
type Query {
  # Health check
  health: String!

  # TODO: Define core domain entities (User, etc.)
}

# Root Mutation type
type Mutation {
  # Placeholder for future mutations
  _empty: String
}

# Root Subscription type (for future real-time updates)
type Subscription {
  # Placeholder for future subscriptions
  _empty: String
}
