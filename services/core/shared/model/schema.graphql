# GraphQL Schema - GRGN Stack Core Shared
# Base types extended by all apps

scalar Time

# Root Query type - extended by apps
type Query {
  # Health check
  health: String!
}

# Root Mutation type - extended by apps
type Mutation {
  _empty: String
}

# Root Subscription type - extended by apps
type Subscription {
  _empty: String
}
